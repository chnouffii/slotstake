/** Ressorts partagés — souplesse organique plutôt que timing mécanique. */
export const SPRING = { type: 'spring', damping: 25, stiffness: 200 };
export const SPRING_SOFT = { type: 'spring', damping: 30, stiffness: 140 };
export const SPRING_SNAP = { type: 'spring', damping: 22, stiffness: 320 };
